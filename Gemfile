source "https://rubygems.org"

# Matches the exact Jekyll + plugin versions GitHub Pages runs, so
# "works on my machine" == "works when deployed".
gem "github-pages", group: :jekyll_plugins

# Needed on Ruby 3.0+ (no longer bundled with Ruby).
gem "webrick", "~> 1.8"
